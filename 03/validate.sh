#!/bin/bash

valid() {
    [[ $# -ne 1 ]] && { echo "Нужен 1 аргумент"; return 1; }
    [[ ! $1 =~ ^[1-3]$ ]] && {
        echo "Выберите способ очистки:"
        echo "1 - По лог-файлу"
        echo "2 - По дате создания"
        echo "3 - По маске имени"
        return 1
    }
    return 0
}
